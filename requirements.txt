Django==5.2.17
django-htmx==1.29.0
python-dotenv==1.2.3
Pillow==12.3.0
django-cleanup==9.0.0
requests==2.34.2
gunicorn==26.2.0
psycopg2-binary>=2.9.0
whitenoise==6.12.0
sqlalchemy>=2,<3
django-redis>=5.4.0
pdfplumber>=0.11.0
anthropic>=0.40.0
openai>=1.0.0
numpy>=1.26.0
djangorestframework>=3.15.0
# sanitiza o HTML do Quill (feed de atividade + descrição) contra XSS armazenado
bleach>=6.1.0
# miniatura (PNG da 1ª página) de PDFs anexados — engine própria, sem poppler
PyMuPDF>=1.24.0

# ============================================================
# Pub/Sub realtime stack (WebSocket via Django Channels + Rabbit bridge)
# ============================================================
channels>=4.1.0
channels-redis>=4.2.0
# redis-py 8.x quebra o channels_redis core: numa conexao WS ociosa a leitura
# do channel layer estoura `TimeoutError: Timeout reading from redis` (~20s) e
# derruba o consumer -> WSDISCONNECT em loop. Fixado no 5.x (comprovado) ate o
# channels_redis suportar o redis-py 8.
redis>=5.0,<6.0
daphne>=4.1.0
pika>=1.3.0
